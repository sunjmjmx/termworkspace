=== Phase2-2: Error Boundary — Invalid API Key ===

Scenario: User enters an invalid or missing API key

Code path: app.py do_send() → ValueError catch

Verified in app.py (lines 741-744):
    except ValueError as e:
        # 配置缺失（无 API key / base URL / 模型名）
        logger.warning("send_message config error: %s", e)
        panel.stream_error(str(e))

Expected user experience:
  1. Panel status shows "thinking" briefly
  2. Error message displayed in conversation: "[Error: ...]"
  3. Panel returns to "idle" state
  4. UI not crashed — user can retry with valid config

Also handles (providers.py lines 83-86):
  - 401 Unauthorized → "API Key无效或已过期"
  - 404 Not Found → "模型不存在或已弃用"
  - 429 Too Many Requests → "请求过频，请稍后再试"
  - 408 Request Timeout → "请求超时"
  - 0 (network error) → "网络连接失败"
