=== Phase2-2: Config Wizard Auto-Launch Evidence ===

Scenario: First-time user runs termworkspace without config.yaml

Code path: app.py main() → ConfigManager.exists() check → auto-launch wizard

Verified:
- Line 847: `if not _CM.exists():` — detects missing ~/.termworkspace/config.yaml
- Line 850-854: User-facing message + auto-launch `_CM.init_wizard()`

Manual invoke supported:
- `termworkspace --init` (__main__.py line 38-41)
- `python3 -m termworkspace --init` (app.py line 827-840)

User feedback:
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    检测到首次运行：未找到配置文件
    将启动配置向导引导您设置 API Key
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

Then prompts for:
  - Provider name (default: ollama)
  - Base URL (default: http://localhost:11434)
  - API Key (optional for local models like Ollama)
  - Model name (default: qwen3.5)

After completion:
  "Configuration complete. Run 'termworkspace' to start."

Resilience:
  ✓ If user skips all providers → app still runs (limited function)
  ✓ If config load fails (corrupt YAML) → warning logged, app continues
