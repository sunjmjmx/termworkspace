=== 场景：空标签状态（Phase 3 - B2 变更） ===
文件：07-empty-tabs.png（待补充真实截图）

触发方式：关闭所有 Tab（连续按 ⌘W 或点 × 按钮，直到最后一个 Tab 也关闭）

界面变化（Phase 3 新行为）：
```
┌──────────────────────────────────────────────┐
│  ~/projects/termworkspace              + │ 🌙 │
├──────────────────────────────────────────────┤
│                                              │
│             没有打开的标签                       │
│        点「+」或按 ⌘N 新建一个                  │
│                                              │
└──────────────────────────────────────────────┘
```

关键变更（B2 fix）：
- 之前：关到最后一个 Tab 时守卫阻止，Tab 无法关闭
- 现在：任何 Tab 都可以关闭，最后一个关掉后显示空状态界面
- 点「+」或 ⌘N 从空状态新建一个终端 Tab

代码路径：
- src/renderer/hooks/useTabState.ts — 移除 length<=1 守卫
- src/renderer/App.tsx — 添加空状态渲染逻辑
- src/renderer/components/TabBar.tsx — 改造，允许渲染空 TabBar

测试命令：打开 3 个 Tab → 逐个关闭到最后一个 → 确认空状态出现 → 点 + 新建
测试结果：✓ 所有 Tab 均可关闭，空状态正确显示，新建 Tab 正常
